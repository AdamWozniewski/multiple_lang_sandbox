export type AttemptStatus = "pending" | "paired" | "approved" | "denied" | "expired";
export enum ATTEMPT_TYPE {
  PENDING = "pending",
  PAIRED = "paired",
  APPROVED = "approved",
  DENIED = "denied",
  EXPIRED = "expired",
}