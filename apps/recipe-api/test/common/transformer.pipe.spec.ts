import { TransformerPipe } from '../../src/common/pipes/transformer/transformer.pipe';

describe('TransformerPipe', () => {
  it('should be defined', () => {
    expect(new TransformerPipe()).toBeDefined();
  });
});
