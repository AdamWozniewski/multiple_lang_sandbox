<% if(name) { %>
    <h1>Firma: <%= name %></h1>
    <h1>Slug : <%= slug %></h1>
    <img src="<%= image %>" alt="">
    <% if(user) { %>
        <a href="/admin/company/<%= name %>/edit">Edytuj</a>
    <% } %>
<% } else { %>
    <h1>Nie ma takiej firmy</h1>
<% } %>