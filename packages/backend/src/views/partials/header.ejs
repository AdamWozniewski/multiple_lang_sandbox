<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/main.css">
    <meta charset="UTF-8">
    <title><%= t('title') ;%>
    </title>
</head>
<body class="h-screen">
<% if(process.env.NODE_ENV === 'development') { %>
    <div class="w-full bg-gray-500">
        <h2 class="text-gray-900 font-medium px-2">ENV: <b><%- process.env.NODE_ENV; %></b></h2>
        <h2 class="text-gray-900 font-medium px-2">GIT_BRANCH: <b><%- process.env.gitBranch; %></b></h2>
    </div>
<% } %>